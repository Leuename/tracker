{
  "name": "@puge/web",
  "private": true,
  "version": "0.6.0",
  "engines": { "node": ">=22" },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/logic.test.js src/rows.test.js src/errors.test.js scripts/rewind-plan.test.js",
    "smoke": "node --env-file-if-exists=.env.local src/smoke.mjs",
    "e2e": "playwright test",
    "security": "node --env-file-if-exists=.env.local security/probe.mjs",
    "backup": "node --env-file-if-exists=.env.local scripts/backup.mjs",
    "schedule": "node --env-file-if-exists=.env.local scripts/schedule.mjs",
    "fx": "node --env-file-if-exists=.env.local scripts/fx.mjs",
    "rewind": "node --env-file-if-exists=.env.local scripts/rewind.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.2.2"
  }
}
